name: Staging E2E Tests
run-name: Staging E2E Tests
on: 
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: NODE_ENV="staging" BROWSER_NAME2="chrome" npx cucumber-js --tags "@int"
